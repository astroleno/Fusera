# Cleanup Backlog

Date: 2026-05-15

## Next Workspace Root Warning

Status: open
Priority: low

`next build` warns that it inferred the workspace root from `/Users/aitoshuu/package-lock.json` while this repo also has `/Users/aitoshuu/Documents/GitHub/Fusera/package-lock.json`.

This does not block the P0 landing-page proof loop, but it should be cleaned up before CI hardening because root inference can make build tracing and cache behavior harder to reason about.

Suggested follow-up:

- Set `outputFileTracingRoot` explicitly in `next.config.ts`, or
- Remove the parent lockfile if it is truly accidental and unrelated.
